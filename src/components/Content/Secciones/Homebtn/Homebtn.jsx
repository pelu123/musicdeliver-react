import React from 'react';

const Homebtn = ({Inicio}) => {
    return (
        <li className="nav-item">
                            <a className="nav-link active" aria-current="page" href="#">Inicio</a>
                        </li>
    );
}

export default Homebtn;